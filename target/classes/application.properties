spring.application.name=algorithm-visualizer
# Railway and similar platforms inject PORT automatically in production.
server.port=${PORT:8080}

# Set this in production to your deployed frontend URL, e.g. https://your-app.vercel.app
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
